Liebe Lincomp-Kundin
Lieber Lincomp-Kunde

<% if @invoice %>
Da bis heute Ihre Zahlung für die Rechnung Nr. <%= "#{@invoice.document_id}" %> 
nicht bei uns eingegangen ist, müssen wir Ihre Bestellung Nr. <%= "#{@order.document_id}" %>
stornieren.

Die dazugehörige Rechnung ist ebenfalls storniert worden und hiermit nichtig.
<% else %>
Da bis heute Ihre Zahlung für die Rechnung zu Bestellung Nr. <%= "#{@order.document_id}" %>
nicht bei uns eingegangen ist, müssen wir die Bestellung stornieren 

Die dazugehörige Rechnung ist ebenfalls storniert worden und hiermit nichtig.
<% end %>

Um solche Fälle in Zukunft zu vermeiden, überweisen Sie bitte den Rechnungs-
betrag in der jeweils angegebenen Frist.

Sie finden die Rechnungsinformationen zur stornierten Rechnung hier, für
Ihre Akten:

<%= url_for :only_path => false, :controller => 'invoices', :action => 'uuid', :uuid => @invoice.uuid %>

Freundliche Grüsse

Lincomp GmbH

--

info@lincomp.ch | http://www.lincomp.ch
