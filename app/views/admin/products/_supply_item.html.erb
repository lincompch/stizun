  <% unless @product.supply_item.nil? %>
  <tr>
    <td>Related supply item</td>
    <td><%= @product.supply_item.to_s %><br />
        Supplier: <%= link_to(@product.supply_item.supplier.name,  admin_supplier_supply_items_path(@product.supplier),{ :target => '_top' }) %>
        </td>
  </tr> 
  
  <% if @product.alternative_supply_items? %>
  <tr>
    <td>Alternative supply items</td>
    <td>
      <ul>
      <% @product.alternative_supply_items.each do |si| %>
        <li><%= si.supplier.name %>: <%= si %> <%= link_to 'Switch to this', switch_to_supply_item_admin_product_path(@product, :supply_item_id => si.id) %></li>
      <% end %>
      </ul>
    </td>
  </tr>
  <% end %>
  
  <% end %>