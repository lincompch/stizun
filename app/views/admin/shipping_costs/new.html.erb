<h1>New shipping_cost</h1>

<%= form_for([:admin, @shipping_cost]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_shipping_costs_path %>