<h1>Edit Supply Item</h1>
<h2><%= @supply_item.name %></h2>


<%= form_for([:admin, @supply_item.supplier, @supply_item]) do |f| %>
<%= render :partial => 'edit_form', :locals => {:f => f} %>

<%= f.submit 'Save' %>
<% end %>
